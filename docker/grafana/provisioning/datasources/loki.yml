apiVersion: 1

datasources:
  - name: Loki
    uid: meditrack-loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: false
    editable: true
    jsonData:
      maxLines: 1000
      derivedFields:
        - name: TraceID
          matcherRegex: 'traceId=([A-Fa-f0-9]+)'
          datasourceUid: meditrack-zipkin
          url: '$${__value.raw}'
          urlDisplayLabel: View Trace
